{
  "name": "type-export",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "copy": "copyfiles -a -F core/dist/ package/core/dist/",
    "build": "npm run build -ws --if-present && npm run copy"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "core",
    "addon",
    "package",
    "consumer",
    "dependency"
  ],
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
